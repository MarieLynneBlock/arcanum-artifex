{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "mkdir-skill",
			"type": "shell",
			"command": "powershell",
			"args": [
				"-Command",
				"New-Item -ItemType Directory -Path '${workspaceFolder}/.github/skills/asset-self-containment-check' -Force"
			]
		}
	]
}